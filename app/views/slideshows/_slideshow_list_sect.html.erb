<div class="container w">
<% if !current_user %>
  <center>
    <a href="#" class='regen_rand_free btn btn-primary btn-lg' data-type='random'>Regen Random Sets</a>
  </center>
<% end %>
    <% slideshows.each do |slideshow| %>
    <% next if !slideshow.public && context_page == 'general' %>
      <div class="col-lg-4 container_for_slideshow_thumb">
        <p>
          <%= slideshow.name %>
          <% if context_page == 'account' %>
            (<a href="#" class='edit-this-show' data-id='<%= slideshow.id %>' class="btn btn-info btn-lg" data-toggle="modal" data-target="#editSlideShowModal">Edit</a> | <a href="#" class='edit-this-show' data-id='<%= slideshow.id %>'>Delete</a>)
          <% end %>
        </p>
        <a href="#" class='start_study' data-id='<%= slideshow.id %>' data-toggle="modal" data-type='indiv' data-target="#drawModal">
          <img src="<%= slideshow.preview %>" class="img-thumbnail img-responsive" alt="<%= slideshow.name %>" width="304" height="236">
        </a>
      </div><!-- col-lg-4 -->
    <% end %>

</div><!-- container -->
<center>
<% if current_user %>
  <div class="pagination_link_container">
    <%= will_paginate slideshows %>
  </div>
<% else %>

<% end %>
</center>
